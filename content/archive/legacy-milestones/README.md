# Legacy Milestone Archive

These JSON files are the archived milestone source that was used to seed the simplified workbook.

- They are not part of the active runtime backend.
- The live tracker now reads only from `content/workbook/AGI benchmark mapping.xlsx`.
- `npm run setup-workbook` rebuilds the workbook from this archive when needed.
